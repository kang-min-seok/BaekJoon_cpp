#include <iostream>
#include <vector>
#include <algorithm>
#include <deque>
using namespace std;


int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int height, width, cut_size;
    cin >> height >> width >> cut_size;

    if(cut_size == 1){
        cout << "0" << "\n";
    } else {
        






    }
    return 0;
}